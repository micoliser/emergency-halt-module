# Studionet only — localnet is not used in this project.
# Direct-mode tests (pytest) do not need funded accounts.
# For Studio smoke / gltest network runs, add studionet accounts here.
networks:
  default: studionet
  studionet:
    url: https://studio.genlayer.com/api

paths:
  contracts: contracts
